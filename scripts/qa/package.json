{
  "name": "moq-qa",
  "private": true,
  "type": "module",
  "description": "Headless Chrome QA harnesses (HLS probing, Zixi I/O matrix) — not part of the production frontend bundle.",
  "scripts": {
    "hls-probe": "node hls-probe.mjs",
    "zixi-io-matrix": "node zixi-io-matrix.mjs"
  },
  "devDependencies": {
    "playwright": "^1.54.2"
  }
}
