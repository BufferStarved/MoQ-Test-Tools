{
  "name": "moq-webcam-qa",
  "private": true,
  "type": "module",
  "description": "Headless Chrome QA harness that drives the real app UI with a fake webcam device against the SRT/RTMP/MoQ comparison flow — not part of the production frontend bundle.",
  "scripts": {
    "webcam-e2e": "node webcam-e2e.mjs"
  },
  "devDependencies": {
    "playwright": "^1.54.2"
  }
}
